
export const Emoji=({label})=>{
	let out=(<span>❌</span>);
	switch (label) {
		case "sender":
			out=(<span>👦</span>);
			break;
		case "reciver":
			out=(<span>👧</span>);
			break;
		case "hacker":
			out=(<span>👺</span>);
			break;
		case "prikey":
			out=(<span style={{filter: "grayscale(100%)"}}>🔑</span>);
			break;
		case "pubkey":
			out=(<span style={{filter: "hue-rotate(90deg)"}}>🔑</span>)
			break;
		case "message":
			out=(<span>📄</span>);
			break;
		case "digest":
			out=(<span>📑</span>)
			break;
		case "signature":
			out=(<span>✒️</span>)
			break;
	}
	return out;
}

# Summaries
## terminology
-	public key <Emoji label="pubkey" />
- private key <Emoji label="prikey" />
-	sender <Emoji label="sender" />
-	reciver <Emoji label="reciver" />
-	hacker <Emoji label="hacker" />
- message <Emoji label="message" />
- digest <Emoji label="digest" />
-	signature <Emoji label="signature	" />

## Category
$$
I = \int_0^{2\pi} \sin(x) dx
$$